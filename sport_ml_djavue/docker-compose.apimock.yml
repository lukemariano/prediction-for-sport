version: "3"

services:

  frontend:
    depends_on:
      - apimock
      - nginx

  apimock:
    build:
      context: ./apimock
    image: apimock-sport_ml_djavue
    container_name: apimock-sport_ml_djavue
    hostname: apimock-sport_ml_djavue
    command: npm run apimock:watch
    network_mode: "host"
    expose:
      - "8000"
    env_file:
      - .env
    volumes:
      - ./apimock/server:/apimock/server
